<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.perusudroid.kotlinbasics">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_launch">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".normal.sample.SampleActivity" />
        <activity android:name=".binding.login.LoginBindActivity" />
        <activity android:name="normal.basic.BasicActivity" />
        <activity android:name=".binding.list.ListActivity" />
        <activity
            android:name=".binding.nav.LaunchBindingActivity"
            android:theme="@style/AppThemeNoAction" />
        <activity android:name=".normal.basic.DataActivity" />
        <activity android:name=".binding.sample.DataActivity"/>
        <activity android:name=".normal.basic.BasicActivity"/>
        <activity android:name=".normal.list.NormalListActivity"/>
    </application>

</manifest>